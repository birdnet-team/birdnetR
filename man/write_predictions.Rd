% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_predictions.R
\name{write_predictions}
\alias{write_predictions}
\alias{write_predictions.birdnet_prediction_geo}
\alias{write_predictions.birdnet_prediction}
\title{Write BirdNET prediction results to file}
\usage{
write_predictions(x, file, format = NULL, ...)

\method{write_predictions}{birdnet_prediction_geo}(x, file, format = NULL, ...)

\method{write_predictions}{birdnet_prediction}(x, file, format = NULL, ...)
}
\arguments{
\item{x}{A BirdNET prediction object (as returned by \code{predict()}).}

\item{file}{Output file path.}

\item{format}{Output file format. If \code{NULL} (default), inferred from
the file extension. Explicit values: \code{"csv"}, \code{"parquet"},
\code{"npz"}.}

\item{...}{Additional arguments passed to the underlying Python method.}
}
\value{
Invisibly returns the output file path.
}
\description{
Write prediction results to disk using the underlying Python result object.
The format can be inferred from the file extension or specified explicitly.
}
\details{
Supported formats by prediction type:
\itemize{
\item \strong{Acoustic predictions}: CSV (\code{.csv}), Parquet (\code{.parquet}), NumPy (\code{.npz})
\item \strong{Geo predictions}: CSV (\code{.csv}), NumPy (\code{.npz})
}
}
\examples{
\dontrun{
write_predictions(pred, "results.csv")
write_predictions(pred, "results.parquet")
write_predictions(pred, "results.npz")
write_predictions(pred, "output.csv", format = "csv")
}
}
