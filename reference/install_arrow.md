# Install Apache Arrow

This helper function installs Apache Arrow for both R and Python.

## Usage

``` r
install_arrow()
```

## Value

Invisible TRUE if successful, stops with error message if installation
fails

## Examples

``` r
if (FALSE) install_arrow() # \dontrun{}
```
