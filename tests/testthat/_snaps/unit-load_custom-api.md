# load_custom errors when species_list is missing

    Code
      load_custom(model = "custom_model.tflite")
    Condition
      Error in `load_custom()`:
      ! Both `model` and `species_list` must be provided.

